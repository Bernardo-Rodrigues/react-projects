import { Category } from '../types/Category';

export const categories: Category = {
    sell: { title: 'Venda', color: 'green', expense: false},
    taken: { title: 'Retirada', color: 'red', expense: true}
}